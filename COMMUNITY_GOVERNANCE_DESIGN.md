# 社区自治审核系统设计

## 核心理念：去中心化的社区治理

### 问题
- 传统管理员审核：中心化、效率低、不透明
- 用户参与度低：只能举报，不能参与决策
- 管理员负担重：所有举报都需要人工审核

### 解决方案：社区投票自治

## 🎯 用户自治审核机制

### 1. 投票权限
**谁可以投票？**
- ⭐ 星光守护者（51+分）及以上
- 🔥 萤火守护者（11+分）可以投票但权重较低
- 🌱 新芽守护者（0-10分）不能投票（防止刷票）

**投票权重**：
- 👑 守护天使（201+分）：权重 3
- ⭐ 星光守护者（51-200分）：权重 2
- 🔥 萤火守护者（11-50分）：权重 1

### 2. 投票机制

**举报提交后**：
1. 进入社区投票阶段（48小时）
2. 所有符合条件的用户可以投票
3. 投票选项：
   - ✅ 支持举报（认为举报有效）
   - ❌ 反对举报（认为举报无效）
   - 🤔 需要更多信息

**自动判定规则**：
- 支持票 × 权重 > 反对票 × 权重 × 1.5 → 举报成立
- 反对票 × 权重 > 支持票 × 权重 × 1.5 → 举报驳回
- 48小时内未达到阈值 → 转交管理员

**快速通道**：
- 如果 5 个守护天使都投支持 → 立即生效
- 如果 10 个星光守护者都投支持 → 立即生效

### 3. 透明度

**公开信息**：
- 当前投票进度
- 投票用户等级分布（不显示具体用户）
- 投票理由（可选填写）
- 实时更新

**隐私保护**：
- 不显示具体投票人
- 只显示等级分布和权重统计

### 4. 激励机制

**投票奖励**：
- 参与投票：+1 分
- 投票结果与最终判定一致：+2 分
- 连续 10 次投票正确：获得"公正裁判"徽章

**惩罚机制**：
- 恶意投票（连续 5 次与最终判定相反）：-5 分
- 刷票行为：封禁投票权限

### 5. 管理员角色转变

**从审核者 → 监督者**：
- 不再审核每个举报
- 只处理争议案件（投票未达成共识）
- 监控投票系统，防止作弊
- 处理申诉

## 实施方案

### Phase 1：基础投票系统（2-3小时）

**后端 API**：
```typescript
// 投票相关
POST /api/reports/:id/vote - 提交投票
GET /api/reports/:id/votes - 查看投票统计
GET /api/reports/pending - 获取待投票的举报

// 自动判定
CRON Job - 每小时检查是否达到判定条件
```

**前端页面**：
- 社区投票页面（/community/votes）
- 举报详情 + 投票按钮
- 投票进度显示

### Phase 2：权重和激励（1-2小时）

- 根据用户等级计算权重
- 投票奖励积分
- 徽章系统集成

### Phase 3：管理员监督（1小时）

- 管理员查看争议案件
- 申诉处理
- 作弊检测

## 优势

### 对用户
- ✅ 参与社区治理
- ✅ 获得积分和徽章
- ✅ 提升参与感和归属感

### 对管理员
- ✅ 减轻审核负担
- ✅ 专注于争议案件
- ✅ 更高效的管理

### 对社区
- ✅ 去中心化治理
- ✅ 透明公开
- ✅ 自我净化能力

## 数据库设计

```sql
-- 投票表
CREATE TABLE report_votes (
  id SERIAL PRIMARY KEY,
  report_id INT NOT NULL,
  user_id INT NOT NULL,
  vote_type VARCHAR(20) NOT NULL, -- 'support', 'oppose', 'need_info'
  vote_weight INT NOT NULL, -- 1, 2, or 3
  reason TEXT,
  created_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(report_id, user_id)
);

-- 更新 reports 表
ALTER TABLE reports ADD COLUMN vote_status VARCHAR(20) DEFAULT 'voting'; -- 'voting', 'approved', 'rejected', 'disputed'
ALTER TABLE reports ADD COLUMN vote_deadline TIMESTAMP;
ALTER TABLE reports ADD COLUMN support_votes INT DEFAULT 0;
ALTER TABLE reports ADD COLUMN oppose_votes INT DEFAULT 0;
```

## UI 设计

### 社区投票页面
```
📊 待投票举报 (3)

[举报卡片]
标记：虐待动物举报
举报原因：信息不实
举报者：用户A

投票进度：
✅ 支持：12 票（权重：18）
❌ 反对：5 票（权重：7）
🤔 需要更多信息：2 票

距离截止：23 小时 45 分钟

[✅ 支持举报] [❌ 反对举报] [🤔 需要更多信息]
```

---

**要现在实现这个社区自治系统吗？** 这将大大提升用户参与度和社区活跃度！
